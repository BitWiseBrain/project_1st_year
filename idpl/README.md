# Biped Robot Control System

A complete end-to-end control system for a bipedal robot with an offline NLP pipeline built from scratch, real VAD, WebSockets, and a terminal dashboard.

## Setup
1. `pip install -r requirements.txt`
2. `python main.py`

Dashboard will open at `http://localhost:8000`.
